/**
 * @deprecated AppMain has been replaced by modern URL-based routing in App.tsx
 * and global Zustand state in useFestivalStore.
 */
import App from '../../app/App';

export default function AppMain() {
  return <App />;
}